# Messaging Service implemented in Node

Currently supports SMS in Production and the rest of channels in Sandbox.

## Run service:
    node src/messaging-server.js

## Installation:
    npm install dotenv express @vonage/server-sdk